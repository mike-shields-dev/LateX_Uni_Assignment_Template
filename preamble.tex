%==============================================================
%BIBLIOGRAPHY
\usepackage[backend=biber, style=authoryear, sorting=ynt]{biblatex}
\addbibresource{references.bib} % Ensure this file exists
%==============================================================


%==============================================================
%FONTS
  %MAIN FONT
  \usepackage{fontspec}
  \setmainfont{Calibri Light} % Set main font to Calibri Light
  % BOLD FONT
  \newfontface\CalibriBold{Calibri Bold}
%==============================================================


%==============================================================
%HEADINGS
\usepackage{titlesec}
%==============================================================


%==============================================================
%MACROS & VARIABLES
  %STUDENT ID
  \newcommand{\studentid}{123456789}
%==============================================================


%==============================================================
%PAGE
  %HEADER/FOOTER
  \usepackage{fancyhdr}
  \pagestyle{fancy}
  \fancyhf{} % Clear existing header and footer
  %HEADER
  \renewcommand{\headrulewidth}{0pt} % Remove horizontal line in header
  \setlength{\headheight}{15pt} % Adjust header height
  \fancyhead[L]{Student ID: \studentid} % Set content for the left of the header
  %FOOTER
  \usepackage{lastpage} % For page number references
  \fancyfoot[C]{Page \thepage\ of \pageref{LastPage}} % Center of footer with page number
  %MARGINS
  \usepackage[a4paper, margin=1in]{geometry}
%==============================================================


%==============================================================
%PARAGRAPH
  %LINE SPACING
  \usepackage{setspace}
  \doublespacing
  %TEXT ALIGNMENT
  \usepackage{ragged2e}
  \justifying % Justify text
  %INDENTATION
  \setlength{\parindent}{0pt}
  \setlength{\parskip}{14pt}
%==============================================================


%==============================================================
%SECTION
  %ADD PAGE BREAKS
  %STYLE & FORMAT
  \usepackage{titlesec}
  \titleformat{\section}
  [hang]
  {\CalibriBold\fontsize{18pt}{14pt}\bfseries\leftskip=0pt}
  {\thesection} % Section number
  {1em}
  {}

  % Ensure sections start on a new page
\usepackage{etoolbox}
\preto\section{\clearpage}
%==============================================================


%==============================================================
%SUBSECTION
  %ADD PAGE BREAKS
  \let\oldsubsection\subsection
  \renewcommand{\subsection}[1]{
    \newpage\oldsubsection{#1}
  }
  %FORMATTING
  \titleformat{\subsection}
  [hang]
  {\CalibriBold\fontsize{16pt}{14pt}\bfseries\leftskip=0pt}
  {\thesubsection}
  {1em}
  {}
%==============================================================


%==============================================================
%SUBSUBSECTION
  %ADD PAGE BREAKS
  \let\oldsubsubsection\subsubsection
  \renewcommand{\subsubsection}[1]{
    \newpage\oldsubsubsection{#1}
  }
  %FORMATTING
  \titleformat{\subsubsection}
  [hang]
  {\CalibriBold\fontsize{14pt}{14pt}\bfseries\leftskip=0pt}
  {\thesubsubsection}
  {1em}
  {}
%==============================================================


%==============================================================
%TABLE OF CONTENTS
\usepackage{tocloft}
  %LINE SPACING
  \setlength{\cftbeforesecskip}{0pt}
  %INCLUSIONS
  \usepackage[nottoc,notlof,notlot]{tocbibind}
%==============================================================


%==============================================================
%HYPERLINKS
  %COLORS
  \usepackage[svgnames]{xcolor}
  \usepackage[
    colorlinks=true, % Enable colored links
    linkcolor=DarkBlue,  % Color for internal links
    urlcolor=DarkBlue,   % Color for URLs
    citecolor=DarkBlue,  % Color for citations
    filecolor=DarkBlue  % Color for file links
  ]{hyperref}
%==============================================================


%==============================================================
%BOOKMARKS
\usepackage{bookmark} % For better bookmark handling
%==============================================================


%==============================================================
%TABLES
  %PADDING
  %HORIZONTAL
  \setlength{\tabcolsep}{12pt} % Horizontal padding
  %VERTICAL
  \renewcommand{\arraystretch}{1.5} % Vertical padding
%==============================================================
